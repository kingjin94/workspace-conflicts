[project]
name = "package-c"
version = "0.1.0"
description = "Package C in the workspace"
requires-python = ">=3.9"

[project.optional-dependencies]
prod = ["psycopg2>=2.9.0"]
non-prod = ["psycopg2-binary>=2.9.0"]

[tool.uv]
conflicts = [
    [
        { extra = "prod" },
        { extra = "non-prod" },
    ],
]

[tool.hatch.build.targets.wheel]
packages = ["src/package_c"]

[build-system]
requires = ["hatchling"]
build-backend = "hatchling.build"
